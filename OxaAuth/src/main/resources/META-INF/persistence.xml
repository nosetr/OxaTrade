<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">
             
<persistence-unit name="mysql-example">
        <provider>org.hibernate.reactive.provider.ReactivePersistenceProvider</provider>

        <class>com.nosetr.auth.entity.NewsletterEntity</class>
        <class>com.nosetr.auth.entity.NewsthemaEntity</class>

        <properties>

            <!-- MySQL -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://${MYSQL_HOST:localhost}:3306/oxatrade?allowPublicKeyRetrieval=true&useSSL=false"/>

            <!-- Credentials -->
            <property name="jakarta.persistence.jdbc.user"
                      value="oxatrade"/>
            <property name="jakarta.persistence.jdbc.password"
                      value="12345678"/>

            <!-- The Vert.x SQL Client connection pool size -->
            <property name="hibernate.connection.pool_size"
                      value="10"/>

            <!-- Automatic schema export -->
            <property name="jakarta.persistence.schema-generation.database.action"
                      value="drop-and-create"/>

            <!-- SQL statement logging -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.highlight_sql" value="true"/>

        </properties>

    </persistence-unit>

</persistence>